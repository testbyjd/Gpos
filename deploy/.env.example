# Copy to deploy/.env and fill in.

# ---- App ----
# Generate once:  docker compose run --rm backend php artisan key:generate --show
APP_KEY=
# Public URL of the VPS (domain if you have one, else http://<vps-ip>).
APP_URL=http://203.0.113.10

# ---- Database ----
DB_DATABASE=gpos
DB_USERNAME=gpos
DB_PASSWORD=change-me-to-something-strong
